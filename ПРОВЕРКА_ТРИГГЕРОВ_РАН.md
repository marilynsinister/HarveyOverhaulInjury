# ✅ Проверка триггеров применения ран

## 📋 Обзор системы

Система применения ран использует **двухуровневую защиту**:
1. **Триггеры** (`AppliedTriggers`) - предотвращают повторное применение одной и той же травмы
2. **Кулдаун** (30 минут) - предотвращает слишком частые травмы

---

## 🔍 Проверка всех мест применения ран

### 1. ProcessDamageBasedInjuries() - Травмы от урона

**Файл:** `PlayerEventHandler.cs:444-492`

**Приоритеты (правильные):**
```
1. health <= 10 → BadlyHurt (100%, приоритет!)
2. damage >= 30 → FracturedBone (5% шанс)
3. damage >= 20 → Concussion (15% шанс)
4. damage >= 15 → BruisedRibs (15% шанс)
5. damage >= 10 → DeepCuts (20% шанс, combat)
6. damage >= 5 → Hurt (25% шанс)
```

**Триггеры:**
- ✅ `Triggers.BadlyHurt`
- ✅ `Triggers.FracturedBone`
- ✅ `Triggers.Concussion`
- ✅ `Triggers.BruisedRibs`
- ✅ `Triggers.DeepCutsCombat`
- ✅ `Triggers.Hurt`

**Статус:** ✅ **ПРАВИЛЬНО** - приоритеты от серьёзных к лёгким, все триггеры определены

---

### 2. CheckExplosionInjuries() - Травмы от взрывов

**Файл:** `PlayerEventHandler.cs:497-546`

**Логика:**
```csharp
if (nearExplosion && Roll(35%))
{
    if (Roll(60%))
        ApplyShrapnelWoundsSafe();  // 60% шанс
    else
        ApplyBurnWoundsSafe();      // 40% шанс
}
```

**Триггеры:**
- ✅ `Triggers.ShrapnelWounds`
- ✅ `Triggers.BurnWounds`

**Статус:** ✅ **ПРАВИЛЬНО** - оба триггера определены

---

### 3. CheckFarmingInjuries() - Травмы от инструментов

**Файл:** `PlayerEventHandler.cs:551-591`

**Логика:**
```csharp
// Back Strain (Hoe/WateringCan, stamina <= 30, 10% шанс)
if (isHoeOrCan && stamina <= 30f && Roll(10%))
    ApplyBackStrainSafe();

// Torn Muscles (Axe/Pickaxe, stamina <= 20, 8% шанс)
if (isAxeOrPick && stamina <= 20f && Roll(8%))
    ApplyTornMusclesSafe();

// Deep Cuts (Scythe/Axe, stamina <= 15, 10% шанс)
if (isScytheOrAxe && stamina <= 15f && Roll(10%))
    ApplyDeepCutsSafe("farming");
```

**Триггеры:**
- ✅ `Triggers.BackStrain`
- ✅ `Triggers.TornMuscles`
- ✅ `Triggers.DeepCutsFarming` ⚠️ **РАЗНЫЙ триггер для farming!**

**Статус:** ✅ **ПРАВИЛЬНО** - разные триггеры для combat и farming

---

### 4. PassOutHandler - Обморок

**Файл:** `PassOutHandler.cs:64`

**Логика:**
```csharp
// При обмороке с критическим здоровьем
if (LastPassedOutHealth <= 15)
    ApplyBadlyHurtSafe();
```

**Триггер:**
- ✅ `Triggers.BadlyHurt`

**Статус:** ✅ **ПРАВИЛЬНО** - использует тот же триггер, что и ProcessDamageBasedInjuries

**⚠️ ВОЗМОЖНАЯ ПРОБЛЕМА:**
Если игрок получил `BadlyHurt` от урона, а потом потерял сознание - триггер уже применён, и `ApplyBadlyHurtSafe()` не сработает повторно.

**Это нормально?** ✅ **ДА** - если уже есть `BadlyHurt`, не нужно применять его повторно.

---

### 5. CheckColdRisk() - Простуда

**Файл:** `PlayerEventHandler.cs:288-295`

**Логика:**
```csharp
if (TimeUnderRainTicks >= 300 && Roll(coldChance))
    ApplyColdSafe();
```

**Триггер:**
- ✅ `"{{ModId}}_triggerCold"` ⚠️ **НЕ в Constants.cs!**

**Статус:** ⚠️ **ТРИГГЕР НЕ В КОНСТАНТАХ** - используется строка напрямую

**Рекомендация:** Добавить в `Constants.cs`:
```csharp
public const string Cold = "{{ModId}}_triggerCold";
```

---

## 📊 Сводная таблица всех триггеров

| Травма | Триггер | Где используется | Статус |
|--------|---------|------------------|--------|
| Hurt | `Triggers.Hurt` | ProcessDamageBasedInjuries | ✅ |
| BadlyHurt | `Triggers.BadlyHurt` | ProcessDamageBasedInjuries, PassOutHandler | ✅ |
| SprainedAnkle | `Triggers.SprainedAnkle` | (не найдено в коде) | ⚠️ |
| BruisedRibs | `Triggers.BruisedRibs` | ProcessDamageBasedInjuries | ✅ |
| BackStrain | `Triggers.BackStrain` | CheckFarmingInjuries | ✅ |
| DeepCuts (combat) | `Triggers.DeepCutsCombat` | ProcessDamageBasedInjuries | ✅ |
| DeepCuts (farming) | `Triggers.DeepCutsFarming` | CheckFarmingInjuries | ✅ |
| BurnWounds | `Triggers.BurnWounds` | CheckExplosionInjuries | ✅ |
| InfectedWound | `Triggers.InfectedWound` | (не найдено в коде) | ⚠️ |
| TornMuscles | `Triggers.TornMuscles` | CheckFarmingInjuries | ✅ |
| Concussion | `Triggers.Concussion` | ProcessDamageBasedInjuries | ✅ |
| FracturedBone | `Triggers.FracturedBone` | ProcessDamageBasedInjuries | ✅ |
| ShrapnelWounds | `Triggers.ShrapnelWounds` | CheckExplosionInjuries | ✅ |
| SurgicalWound | `Triggers.SurgicalWound` | (не найдено в коде) | ⚠️ |
| Cold | `Triggers.Cold` | CheckColdRisk | ✅ **ИСПРАВЛЕНО** |

---

## 🔧 Логика ApplyInjurySafe()

**Файл:** `InjuryManager.cs:310-344`

**Проверки:**
1. ✅ `WasApplied(triggerConst)` - триггер уже применён?
2. ✅ `CanApplyInjury()` - прошло 30 минут с последней травмы?
3. ✅ Применение травмы
4. ✅ `MarkApplied(triggerConst)` - пометить триггер как применённый
5. ✅ `UpdateLastInjuryTime()` - обновить время последней травмы

**Защита от ошибок:**
- ✅ Try-catch блок
- ✅ В случае ошибки триггер всё равно помечается как применённый (избегаем зацикливания)

**Статус:** ✅ **ПРАВИЛЬНО** - логика корректна

---

## ⚠️ Найденные проблемы

### 1. ✅ ИСПРАВЛЕНО: Триггер простуды не в Constants.cs

**Было:**
```csharp
// InjuryManager.cs:616
ApplyInjurySafe(InjuryBuffs.Cold, ApplyCold, "{{ModId}}_triggerCold");
```

**Исправлено:**
1. ✅ Добавлено в `Constants.cs`:
```csharp
public static class Triggers
{
    // ... существующие ...
    public const string Cold = "{{ModId}}_triggerCold";
}
```

2. ✅ Используется константа:
```csharp
ApplyInjurySafe(InjuryBuffs.Cold, ApplyCold, Triggers.Cold);
```

**Статус:** ✅ **ИСПРАВЛЕНО**

---

### 2. Некоторые травмы не используются в коде

**Не найдены применения:**
- `SprainedAnkle` - триггер есть, но не используется
- `InfectedWound` - триггер есть, но не используется
- `SurgicalWound` - триггер есть, но не используется

**Статус:** ⚠️ **НЕ КРИТИЧНО** - возможно, применяются через другие механизмы или будут добавлены позже

---

## ✅ Рекомендации

### 1. ✅ ВЫПОЛНЕНО: Добавить триггер простуды в Constants.cs

**Исправлено:**
- ✅ Добавлен `Triggers.Cold` в `Constants.cs`
- ✅ Используется константа вместо строки в `InjuryManager.cs`

---

## 📊 Итоговая оценка

| Критерий | Статус | Комментарий |
|----------|--------|-------------|
| Приоритеты травм | ✅ | Правильно: от серьёзных к лёгким |
| Триггеры для всех ран | ✅ | Все основные триггеры определены |
| Кулдаун | ✅ | 30 минут между травмами |
| Защита от дубликатов | ✅ | Триггеры предотвращают повторное применение |
| Обработка ошибок | ✅ | Try-catch с безопасным завершением |
| Константы | ✅ | Все триггеры в Constants.cs |

**Общая оценка:** ✅ **ОТЛИЧНО** (все проблемы исправлены)

---

## 🎯 Выводы

1. ✅ **Логика приоритетов правильная** - более серьёзные травмы применяются первыми
2. ✅ **Триггеры работают корректно** - предотвращают повторное применение
3. ✅ **Кулдаун активен** - 30 минут между травмами
4. ✅ **Все триггеры в Constants.cs** - константы используются везде

**Система работает правильно!** 🎉

